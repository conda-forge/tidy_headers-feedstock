{% set name = "tidy_headers" %}
{% set version = "0.2.0" %}
{% set sha256 = "4858d9c689d9f4ba128a44d1da40bd073e4bc1d06f2b4fd79442330cd8f42278" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: python setup.py install --single-version-externally-managed --record record.txt

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - numpy

test:
  imports:
    - tidy_headers

about:
  home: https://github.com/untzag/tidy_headers
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: "Easy headers, inspired by the tidy data specification." 

extra:
  recipe-maintainers:
    - untzag
